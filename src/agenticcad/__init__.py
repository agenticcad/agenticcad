"""Desktop launcher for AgenticCAD (packaged with Briefcase). The app itself lives in the top-level modules
(server.py, agent.py, cad_kernel.py …); this package only wires them into a native window."""
